---
# Default variables - inventory or playbook overrides will be respected

bootstrap_vagrant_controller_user_username: controller
bootstrap_vagrant_controller_user_enabled: true

bootstrap_vagrant_controller_user_authorized_keys_directory: ../../../public_keys
